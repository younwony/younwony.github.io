# 알고리즘 문제 풀이 완벽 정리

> **출처**: LeetCode, Programmers
> **언어**: Java
> **문제 수**: LeetCode 7개, Programmers 2개
> **학습 기간**: 2020.11 ~ 2021.03

---

## 📚 목차

1. [개요](#개요)
2. [LeetCode 풀이 (7개)](#leetcode-풀이-7개)
3. [Programmers 풀이 (2개)](#programmers-풀이-2개)
4. [핵심 패턴 정리](#핵심-패턴-정리)
5. [Java 활용 팁](#java-활용-팁)

---

## 개요

알고리즘 학습 초기 단계에서 Easy 난이도 위주로 풀이한 문제들입니다. 기본적인 자료구조와 알고리즘 활용법을 익히는 데 중점을 두었습니다.

### 학습 목표
- Java 기본 문법 숙달
- 배열, 문자열, 루프 등 기초 개념 이해
- 시간/공간 복잡도 분석 연습

---

## LeetCode 풀이 (7개)

### 1. Reverse Integer ⭐
**문제**: [LeetCode 7](https://leetcode.com/problems/reverse-integer/)
**난이도**: Easy
**카테고리**: 문자열 조작

#### 핵심 아이디어
```java
StringBuilder를 활용한 문자열 뒤집기
String reversed = new StringBuilder(str).reverse().toString();
```

**시간 복잡도**: O(n)

---

### 2. Palindrome Number ⭐
**문제**: [LeetCode 9](https://leetcode.com/problems/palindrome-number/)
**난이도**: Easy
**카테고리**: 문자열 비교

#### 풀이 전략
1. 정수를 문자열로 변환
2. 뒤집은 문자열과 비교
3. 같으면 회문(Palindrome)

**대안**: 수학적 접근 (문자열 변환 없이 숫자 조작)

---

### 3. Running Sum of 1d Array ⭐
**문제**: [LeetCode 1480](https://leetcode.com/problems/running-sum-of-1d-array/)
**난이도**: Easy
**카테고리**: 배열, 누적 합

#### 알고리즘
```java
// 누적 합 계산
for (int i = 1; i < nums.length; i++) {
    nums[i] += nums[i-1];
}
```

**시간 복잡도**: O(n)
**공간 복잡도**: O(1) (in-place 수정)

---

### 4. Richest Customer Wealth ⭐⭐
**문제**: [LeetCode 1672](https://leetcode.com/problems/richest-customer-wealth/)
**난이도**: Easy
**카테고리**: 2D 배열, 최대값

#### 코드 예제
```java
public int maximumWealth(int[][] accounts) {
    int max = 0;
    for (int[] account : accounts) {
        int sum = 0;
        for (int i : account) {
            sum += i;
        }
        max = Math.max(max, sum);
    }
    return max;
}
```

**패턴**: 중첩 루프 + Math.max 활용

---

### 5. Kids With the Greatest Number of Candies ⭐⭐
**문제**: [LeetCode 1431](https://leetcode.com/problems/kids-with-the-greatest-number-of-candies/)
**난이도**: Easy
**카테고리**: 배열, 비교 연산

#### 접근 방법
1. 배열에서 최대값 찾기
2. 각 원소 + 추가값 >= 최대값 비교
3. Boolean 리스트 반환

**활용 메서드**: `Arrays.stream().max()`

---

### 6. Shuffle the Array ⭐⭐
**문제**: [LeetCode 1470](https://leetcode.com/problems/shuffle-the-array/)
**난이도**: Easy
**카테고리**: 배열 재배치

#### 핵심 로직
```java
public int[] shuffle(int[] nums, int n) {
    int[] result = new int[nums.length];
    for (int i = 0; i < n; i++) {
        result[2*i] = nums[i];      // x_i
        result[2*i+1] = nums[i+n];  // y_i
    }
    return result;
}
```

**패턴**: 인덱스 계산 (2*i, 2*i+1)

---

### 7. Number of Good Pairs ⭐⭐
**문제**: [LeetCode 1512](https://leetcode.com/problems/number-of-good-pairs/)
**난이도**: Easy
**카테고리**: 배열, 완전 탐색

#### 기본 풀이 (O(n²))
```java
public int numIdenticalPairs(int[] nums) {
    int count = 0;
    for(int i = 0; i < nums.length; i++){
        for(int j = i+1; j < nums.length; j++){
            if(nums[i] == nums[j]) count++;
        }
    }
    return count;
}
```

#### 최적화 풀이 (O(n))
HashMap을 활용한 빈도 수 계산

---

## Programmers 풀이 (2개)

### 1. 다리를 지나는 트럭 ⭐⭐⭐
**문제**: [Programmers Level 2](https://programmers.co.kr/learn/courses/30/lessons/42583)
**난이도**: Level 2
**카테고리**: 큐(Queue), 시뮬레이션

#### 핵심 개념
- **자료구조**: Queue (LinkedList)
- **알고리즘**: 시간 단위 시뮬레이션
- **관리 요소**: 다리 위 트럭 무게, 시간

#### 풀이 전략
1. 큐로 다리 상태 관리
2. 매 초마다 상태 업데이트
3. 무게 제한 검사 후 트럭 진입

**난이도**: LeetCode Easy보다 한 단계 높은 수준

---

### 2. 신규아이디 추천 ⭐⭐⭐
**문제**: [Programmers Level 1](https://programmers.co.kr/learn/courses/30/lessons/72410)
**난이도**: Level 1 (2021 카카오 블라인드)
**카테고리**: 문자열 처리, 정규식

#### 7단계 로직
1. **1단계**: 대문자 → 소문자
2. **2단계**: 허용 문자 외 제거 (소문자, 숫자, `-`, `_`, `.`)
3. **3단계**: 연속된 마침표 → 단일 마침표
4. **4단계**: 시작/끝 마침표 제거
5. **5단계**: 빈 문자열이면 `"a"` 대입
6. **6단계**: 16자 이상 → 15자로 자르고 끝 마침표 제거
7. **7단계**: 2자 이하 → 마지막 문자 반복 추가

#### 구현 방법
- **기본**: 문자열 메서드 활용
- **고급**: 정규식 활용 (더 간결)

```java
// 정규식 예시
str = str.replaceAll("[^a-z0-9-_.]", "");  // 2단계
str = str.replaceAll("\\.+", ".");         // 3단계
```

---

## 핵심 패턴 정리

### 자주 사용되는 패턴

| 패턴 | 활용 문제 | 시간 복잡도 |
|------|----------|------------|
| **중첩 루프 비교** | Number of Good Pairs | O(n²) |
| **누적 합** | Running Sum | O(n) |
| **최대값 찾기** | Richest Customer Wealth | O(n) 또는 O(nm) |
| **문자열 조작** | 신규아이디, Palindrome | O(n) |
| **큐 시뮬레이션** | 다리를 지나는 트럭 | O(n*m) |
| **인덱스 계산** | Shuffle Array | O(n) |

### 알고리즘 분류

```
기초 문제 (LeetCode Easy)
├── 배열 조작: Running Sum, Shuffle, Richest Customer
├── 문자열: Reverse Integer, Palindrome
└── 비교 연산: Kids With Candies, Good Pairs

중급 문제 (Programmers)
├── 자료구조: 다리를 지나는 트럭 (Queue)
└── 문자열 처리: 신규아이디 추천 (정규식)
```

---

## Java 활용 팁

### 유용한 메서드

#### 배열/리스트
```java
// 최대값 찾기
Math.max(a, b);
Arrays.stream(arr).max().getAsInt();

// 배열 순회
for (int num : nums) { }

// ArrayList
List<Boolean> list = new ArrayList<>();
list.add(true);
```

#### 문자열
```java
// 뒤집기
String reversed = new StringBuilder(str).reverse().toString();

// 대소문자 변환
str.toLowerCase();
str.toUpperCase();

// 문자열 → 숫자
Integer.parseInt(str);

// 문자 추출
char c = str.charAt(i);
```

#### 정규식
```java
// 패턴 매칭
str.matches("[a-z]+");

// 문자 치환
str.replaceAll("[^a-z0-9]", "");

// 연속 문자 제거
str.replaceAll("\\.+", ".");
```

### 자료구조 선택 가이드

| 상황 | 추천 자료구조 | 예시 문제 |
|------|-------------|----------|
| 순서 유지 + 빠른 접근 | ArrayList | Running Sum |
| FIFO 처리 | Queue (LinkedList) | 다리를 지나는 트럭 |
| 빈도 수 계산 | HashMap | Good Pairs 최적화 |
| 정렬 필요 | TreeMap, PriorityQueue | - |

---

## 학습 포인트

### 문제 풀이 체크리스트
1. ✅ 입출력 예제 확인
2. ✅ 시간/공간 제약 파악
3. ✅ 엣지 케이스 고려 (빈 배열, null 등)
4. ✅ 최적화 가능성 검토

### 난이도별 학습 전략

#### Easy (LeetCode)
- 기본 문법 숙달
- 단순 루프, 조건문 활용
- 시간 복잡도: O(n) ~ O(n²)

#### Level 1-2 (Programmers)
- 자료구조 활용 (Queue, Stack)
- 시뮬레이션 구현
- 문자열 처리 고급 기법

---

## 실무 적용 가이드

### 바로 적용 가능한 패턴
1. ✅ 누적 합 계산 (통계 처리)
2. ✅ 최대/최소값 찾기 (데이터 분석)
3. ✅ 문자열 정제 (사용자 입력 검증)
4. ✅ 큐 기반 처리 (작업 스케줄링)

### 추가 학습 방향
1. 📋 HashMap 활용 최적화
2. 📋 정규식 마스터
3. 📋 재귀 알고리즘
4. 📋 동적 계획법 입문

---

## 참고 자료

- **LeetCode**: https://leetcode.com/
- **Programmers**: https://programmers.co.kr/
- **원본 블로그**: https://youn12.tistory.com/category/Algorithm

---

**작성일**: 2025-11-30
**기반**: 12Dev 블로그 Algorithm 9개 포스팅
