# 객체지향의 사실과 오해 완벽 정리

> **원서**: 객체지향의 사실과 오해
> **저자**: 조영호
> **학습 기간**: 2023.01.17 ~ 2023.01.31 (2주)
> **포스팅 수**: 2개

---

## 📚 목차

1. [스터디 개요](#스터디-개요)
2. [1장 - 협력하는 객체들의 공동체](#1장---협력하는-객체들의-공동체)
3. [2장 - 이상한 나라의 객체](#2장---이상한-나라의-객체)
4. [핵심 원칙 정리](#핵심-원칙-정리)
5. [실무 적용 가이드](#실무-적용-가이드)

---

## 스터디 개요

### 책 소개
조영호 저자의 **"객체지향의 사실과 오해"**는 객체지향 프로그래밍의 본질에 대해 깊이 있게 설명하는 책입니다.

### 선정 이유
- 객체지향의 정의와 본질을 이해하기 위함
- 좋은 설계 방법론 학습
- 클래스가 아닌 객체 중심 사고 훈련

### 학습 목표
- 객체지향의 진정한 의미 파악
- 역할, 책임, 협력 개념 이해
- 책임-주도 설계(RDD) 습득

---

## 1장 - 협력하는 객체들의 공동체

**작성일**: 2023.01.17

### 핵심 메시지
> 객체지향의 목표는 **"새로운 세계를 창조"**하는 것

실세계의 모방이 아니라, 새로운 세계를 창조하는 것이 객체지향의 본질입니다.

---

### 역할, 책임, 협력

#### 1. 협력 (Collaboration)
> "객체들이 애플리케이션의 기능을 구현하기 위해 수행하는 상호작용"

객체는 혼자서는 아무것도 할 수 없으며, 다른 객체와 협력해야만 목표를 달성할 수 있습니다.

#### 2. 책임 (Responsibility)
> "적절한 객체에게 적절한 책임을 할당하는 것이 객체지향 설계의 시작점"

**책임의 분류**:
- **하는 것 (doing)**: 객체를 생성하거나 계산을 수행하는 등의 스스로 하는 것
- **아는 것 (knowing)**: 개인적인 정보에 관해 아는 것

#### 3. 역할 (Role)
> "유사한 책임을 수행하는 객체들의 추상화"

역할을 통해 설계를 유연하고 재사용 가능하게 만들 수 있습니다.

---

### 협력 속에 사는 객체

객체의 두 가지 중요한 특징:

#### 1. 협력적 (Cooperative)
- 다른 객체의 요청에 응답
- 메시지를 통해 소통

#### 2. 자율적 (Autonomous)
- 자신의 방식대로 요청 처리
- 내부 구현을 스스로 결정

```
협력적이면서도 자율적인 객체
     ↓
캡슐화를 통한 정보 은닉
     ↓
변경에 유연한 설계
```

---

### 객체지향의 본질

#### 핵심 정의
> **"객체지향은 객체를 지향하는 것이지 클래스를 지향하는 것이 아니다"**

많은 개발자들이 클래스에 집중하지만, 진정한 객체지향은 **객체**에 초점을 맞춰야 합니다.

#### 객체지향 시스템의 특징
1. 자율적 객체들의 공동체로 시스템을 바라봄
2. 메시지 전송을 통한 협력
3. 각 객체는 고유한 역할과 책임을 가짐

#### 설계의 핵심
```
역할 → 책임 → 협력
  ↓      ↓      ↓
추상화  캡슐화  다형성
```

---

## 2장 - 이상한 나라의 객체

**작성일**: 2023.01.17

### 객체의 핵심 특징

#### 1. 상태 (State)
- 객체는 상태를 가짐
- 상태는 변경 가능 (mutable)
- 과거의 행동이 현재의 상태를 결정

#### 2. 행동 (Behavior)
- 상태를 변경하는 유일한 방법
- 외부에서 직접 상태 변경 불가
- 행동을 통해서만 상태 조작

#### 3. 식별자 (Identity)
- 객체를 구별하는 유일한 값
- 동등성(equality)과 동일성(identity) 구분

---

### 소프트웨어 세계의 객체

#### 객체의 구성 요소
```
객체
├── 상태 (프로퍼티)
│   ├── 단순한 값
│   └── 다른 객체에 대한 참조
├── 행동 (메서드)
│   ├── 상태 조회
│   └── 상태 변경
└── 식별자 (ID)
```

#### 캡슐화의 중요성
> "객체의 상태를 변경하는 것은 객체의 자발적인 행동뿐"

외부에서 직접 상태를 변경할 수 없고, 메시지를 보내 행동을 요청해야 합니다.

---

### 설계의 핵심 원칙

#### 행동이 상태를 결정한다 ⭐⭐⭐

| 설계 방식 | 접근 | 문제점 |
|----------|------|--------|
| **상태 중심 설계** | 상태를 먼저 결정 | 캡슐화 위반, 변경에 취약 |
| **책임-주도 설계** | 행동(책임)을 먼저 결정 | 유연하고 확장 가능 |

```java
// ❌ 상태 중심 설계 (나쁜 예)
class User {
    public String name;  // public 필드
    public int age;
}

// ✅ 책임-주도 설계 (좋은 예)
class User {
    private String name;
    private int age;

    public void changeName(String newName) {  // 행동으로 상태 변경
        this.name = newName;
    }

    public boolean isAdult() {  // 행동으로 판단
        return age >= 19;
    }
}
```

---

### 동등성 vs 동일성

#### 동등성 (Equality)
- **비교 대상**: 값 (상태)
- **비교 방식**: 상태가 같은지
- **Java 메서드**: `equals()`

```java
String a = new String("hello");
String b = new String("hello");
a.equals(b);  // true (동등)
```

#### 동일성 (Identity)
- **비교 대상**: 객체
- **비교 방식**: 같은 객체인지
- **Java 메서드**: `==`

```java
String a = new String("hello");
String b = new String("hello");
a == b;  // false (다른 객체)
```

| 구분 | 값 객체 (VO) | 엔티티 (Entity) |
|------|------------|---------------|
| 비교 기준 | 동등성 | 동일성 |
| 가변성 | 불변 | 가변 |
| 예시 | Money, Point | User, Order |

---

## 핵심 원칙 정리

### 1. 객체지향의 본질

#### 핵심 정의
```
객체지향 = 역할 + 책임 + 협력
```

- 클래스가 아닌 **객체**를 중심으로 생각
- 상속보다 **협력**에 초점
- 데이터가 아닌 **행동**이 우선

---

### 2. 좋은 설계의 조건

#### 책임-주도 설계 (RDD)

| 단계 | 질문 | 결과 |
|------|------|------|
| 1. 시스템 기능 | 무엇을 해야 하는가? | 책임 식별 |
| 2. 책임 할당 | 누가 해야 하는가? | 객체 선택 |
| 3. 협력 설계 | 어떻게 협력하는가? | 메시지 정의 |

```
1. 책임 식별
    ↓
2. 적절한 객체에게 책임 할당
    ↓
3. 메시지를 통한 협력 관계 설계
    ↓
4. 구현 (클래스 작성)
```

---

### 3. 객체의 자율성

#### 자율성을 높이는 방법

1. **캡슐화**
   ```java
   // ❌ 캡슐화 위반
   if (user.age >= 19) { }

   // ✅ 캡슐화 준수
   if (user.isAdult()) { }
   ```

2. **정보 은닉**
   - private 필드
   - public 메서드 (행동)

3. **메시지 중심 설계**
   - 구현이 아닌 인터페이스에 집중
   - "어떻게"가 아닌 "무엇을"

---

### 4. 역할과 책임

#### 역할의 장점
- **추상화**: 세부 사항 숨김
- **유연성**: 역할 대체 가능
- **재사용성**: 다양한 객체가 같은 역할 수행

```java
// 역할 (인터페이스)
interface PaymentMethod {
    void pay(int amount);
}

// 다양한 구현체
class CreditCard implements PaymentMethod { }
class Cash implements PaymentMethod { }
class Coupon implements PaymentMethod { }

// 사용 (역할에 의존)
void checkout(PaymentMethod method, int amount) {
    method.pay(amount);  // 구체적인 구현 몰라도 됨
}
```

---

## 실무 적용 가이드

### 즉시 적용 가능

#### 1. ✅ 데이터가 아닌 책임 중심 설계
```java
// Before: 데이터 중심
class Order {
    public List<Item> items;
    public int totalPrice;
}

// After: 책임 중심
class Order {
    private List<Item> items;

    public int calculateTotal() {
        return items.stream()
            .mapToInt(Item::getPrice)
            .sum();
    }

    public void addItem(Item item) {
        items.add(item);
    }
}
```

#### 2. ✅ Tell, Don't Ask 원칙
```java
// Before: 물어보고 판단 (Ask)
if (user.getAge() >= 19) {
    allowPurchase();
}

// After: 시키기만 함 (Tell)
if (user.isAdult()) {
    allowPurchase();
}
```

#### 3. ✅ 인터페이스를 통한 역할 정의
```java
// 역할 정의
interface Notifier {
    void notify(String message);
}

// 다양한 구현
class EmailNotifier implements Notifier { }
class SmsNotifier implements Notifier { }
class PushNotifier implements Notifier { }
```

---

### 팀 단위 적용

#### 1. 📋 협력 다이어그램 작성
- 객체 간 협력 관계 시각화
- 책임 분배 명확화

#### 2. 📋 CRC 카드 활용
```
[Class Name]
-----------------
Responsibilities:
- 책임 1
- 책임 2

Collaborators:
- 협력 객체 1
- 협력 객체 2
```

#### 3. 📋 코드 리뷰 체크리스트
- [ ] 객체가 책임을 갖고 있는가?
- [ ] 캡슐화가 잘 되어 있는가?
- [ ] 역할과 구현이 분리되어 있는가?
- [ ] Tell, Don't Ask 원칙을 따르는가?

---

### 장기 프로젝트 적용

#### 1. 🎯 도메인 모델 설계
- 역할과 책임 명확히 정의
- 협력 관계 먼저 설계

#### 2. 🎯 리팩터링 전략
```
1. 데이터 중심 → 책임 중심
2. getter/setter 남발 제거
3. 행동(메서드) 추가
4. 협력 관계 개선
```

#### 3. 🎯 지속적 개선
- 정기적인 설계 리뷰
- 객체 역할 재평가
- 협력 관계 최적화

---

## 학습 회고

### 긍정적 평가
✅ **객체지향 본질 이해**
- 클래스가 아닌 객체 중심 사고
- 역할, 책임, 협력의 중요성 인식

✅ **설계 철학 습득**
- 책임-주도 설계(RDD) 이해
- 행동이 상태를 결정한다는 원칙

✅ **실무 적용 가능**
- Tell, Don't Ask 원칙
- 캡슐화와 정보 은닉

---

### 핵심 교훈

> **"객체지향은 객체를 지향하는 것이지 클래스를 지향하는 것이 아니다"**

> **"행동이 상태를 결정한다"**

> **"적절한 객체에게 적절한 책임을 할당하는 것이 설계의 시작"**

---

## 참고 자료

- **원서**: 객체지향의 사실과 오해
- **저자**: 조영호
- **출판사**: 위키북스
- **원본 블로그**: https://youn12.tistory.com/category/Study

---

**작성일**: 2025-11-30
**기반**: 12Dev 블로그 OOP 스터디 2개 포스팅
