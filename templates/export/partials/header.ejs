<header class="header">
    <h1>경력기술서</h1>
    <div class="subtitle"><%= profile.name %> | <%= profile.title %> | <%= profile.years %></div>
    <div class="contact-row">
        <span><i class="fas fa-envelope"></i><%= profile.email %></span>
        <span><i class="fas fa-phone"></i><%= profile.phone %></span>
        <span><i class="fab fa-github"></i><%= profile.github %></span>
    </div>
</header>
